import * as React from "react";

const IndexPage = () => {
  return <main>This will contain the main content</main>;
};

export default IndexPage;

export const Head = () => <title>Home Page</title>;
